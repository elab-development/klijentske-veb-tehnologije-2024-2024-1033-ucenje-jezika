-- installing package in terminal
npm install -g firebase -tools

-- local log in to firebase
firebase login

-- selecting what we need (firestore, hosting etc.)
-- selecting project or creating a new one
-- we get new files and folder build


-- deploying rules
firebase deploy --only firestore